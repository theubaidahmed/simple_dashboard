import { createTheme } from '@mui/material';

const theme = createTheme({
    palette: {
        primary: {
            main: '#353A55',
        },
        secondary: {
            main: '#738CF2',
        },
        tertiary: {
            main: '#FD9898',
        },
        background: {
            default: '#EBEEF4',
        },
    },
});

export default theme;
